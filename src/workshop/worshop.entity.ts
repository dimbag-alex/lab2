export class WorkShop {
    id: number;
    fullname: string;
    address: string;
    employers: string[]; // потом будет Employer[]
    services: string[]; // потом будет Service[]
    orders: string[]; // потом будет Order[]
  }